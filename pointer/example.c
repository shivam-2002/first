#include<stdio.h>
per(float *x);
main()
{
    int y;
    float *x,s[5];
    for(y=0;y<5;y++)
    {
     scanf("%f",&s[x]);
    }
    x=s;
    per(x);//function clall
}
per(flat *x)
{
    int a,totalmarks=0,percentage;
    for(a=0;a<5;a++)
    {
       totalmarks=totalmarks+ *x;
       p++;
    }
    percentage=(totalmarks*100)/500;

}

